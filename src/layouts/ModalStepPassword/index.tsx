import ModalStep from "../../components/ModalStep";
import React, {useState} from "react";

const ModalStepPassword = () => {
  const [step, setStep] = useState(0);

  return (
    <ModalStep>

    </ModalStep>
  )
}
export default ModalStepPassword;
